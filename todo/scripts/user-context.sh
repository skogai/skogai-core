#!/usr/bin/env bash
# Append context to user's message (UserPromptSubmit)
# stdout is injected as additionalContext alongside the user's prompt
# Edit this script to add whatever you want Claude to see with each message

# Add context here — stdout gets injected with every user message
# Example: echo "Current branch: $(git branch --show-current 2>/dev/null)"
